rules:
  - /tmp/bocchi:
      - /usr/bin/cat:
          user: paml
          log:
            - open
            - read
            - write
      - /usr/bin/cp:
          user: paml
      - /usr/bin/xxd:
          user: paml
      - /usr/bin/nvim:
          user: paml
  - /tmp/hosts:
      - /usr/bin/cat:
          user: paml
          log:
            - open
            - read
            - write
      - /usr/bin/cp:
          user: paml
      - /usr/bin/xxd:
          user: paml
